version: '3'
services:
  app:
    build: .
    ports:
      - 5000:5000

  mysql:
    image: mysql:8.0.33
    environment:
      MYSQL_ROOT_PASSWORD: adrien
    volumes:
      - ./mysql_volume:/var/lib/mysql


  phpmyadmin:
    image: phpmyadmin:latest
    ports:
      - 8080:80
    environment:
      - PMA_ARBITRARY=1

volumes:
  data:
